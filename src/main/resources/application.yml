spring:
  application:
    name: smart-learnly-backend

server:
  port: ${SERVER_PORT:8080}

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173}
  security:
    jwt-secret: ${JWT_SECRET:}
  integrations:
    ai-provider: ${AI_PROVIDER:none}
    payment-provider: ${PAYMENT_PROVIDER:none}

# Local development PostgreSQL for Smart Learnly Backend.
# Copy to .env and adjust values. .env is gitignored; do not commit credentials.
POSTGRES_DB=smartlearnly_schema_retest
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me-to-a-local-only-password
POSTGRES_PORT=55432
